#!/bin/bash -e
cat <<EOF
This is the registry.access.redhat.com/redhat-openjdk-18/openjdk18-openshift S2I image:
To use it, install S2I: https://github.com/openshift/source-to-image

Sample invocation:

s2i build <source code path/URL> registry.access.redhat.com/redhat-openjdk-18/openjdk18-openshift <application image>

You can then run the resulting image via:
docker run <application image>
EOF
